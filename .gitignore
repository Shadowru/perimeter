__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
/models/          # наши веса; якорь важен: без него правило вырезало
                  # исходники vendor/llama.cpp/tools/mtmd/models
*.gguf

# Рантайм-состояние движка (кэш KV и статистика экспертов) — не версионируем
.coli_kv*
.coli_usage

# Рабочий конфиг заказчика: создаётся установщиком из perimeter.example.yaml.
# Не версионируется, иначе обновление продукта конфликтует с локальными правками.
/config/perimeter.yaml

# Журнал аудита — данные заказчика: вопросы человека и содержимое 1С.
# Попал в коммит через `git add -A` (31.07) — теперь исключён явно.
/var/
